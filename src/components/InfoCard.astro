---
interface Props {
  title: string;
  href: string;
}

const { title, href } = Astro.props;
---

<article class="info-card">
  <h3>{title}</h3>
  <p><slot /></p>
  <a href={href} class="btn btn-small">More info</a>
</article>
